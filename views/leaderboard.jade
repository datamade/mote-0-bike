div.content
    div.row
        each user in users
          div.span3.well
            h3 #{user.mail}
            a(href="/user/#{user._id}")
              if user.trips.length == 1
                h4 #{user.trips.length} trip
              else
                h4 #{user.trips.length} trips